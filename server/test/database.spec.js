const { connectDB, disconnectDB } = require('../database/helpers');

describe('Database', () => {
  describe('helpers.js', () => {
    it('should throw an error when the connection fails', () => {});
    it('should not throw an error when the connection succeeds', () => {});
  });
});
